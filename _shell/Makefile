PREFIX ?= /usr/local

all:
	@echo "No need to build, please use 'make install'"
	@echo "default PREFIX is ${PREFIX} and needs root"

install:
	install -d ${BUILD}${PREFIX}/bin
	install -p markdown-inline-tag ${BUILD}${PREFIX}/bin/markdown-inline-tag
